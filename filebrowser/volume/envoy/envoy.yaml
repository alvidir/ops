admin:
  address:
    socket_address: { address: ${ENVOY_ADMIN_ADDR}, port_value: ${ENVOY_ADMIN_PORT} }

static_resources:
  listeners:
    - name: listener_0
      address:
        socket_address: { address: ${ENVOY_ENTRY_ADDR}, port_value: ${ENVOY_ENTRY_PORT} }
      filter_chains:
        - filters:
            - name: envoy.filters.network.http_connection_manager
              typed_config:
                "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                codec_type: auto
                stat_prefix: ingress_http
                route_config:
                  name: local_route
                  virtual_hosts:
                    - name: local_service
                      domains: ["*"]
                      routes:
                        - match:
                            prefix: "/"
                            grpc:
                          route:
                            cluster: ${ENVOY_TARGET_NAME}
                      cors:
                        allow_origin_string_match:
                          - prefix: "*"
                        allow_methods: GET, PUT, DELETE, POST, OPTIONS
                        allow_headers: ${ENVOY_ALLOW_HEADERS},keep-alive,user-agent,cache-control,content-type,content-transfer-encoding,grpc-status-details-bin,x-accept-content-transfer-encoding,x-accept-response-streaming,x-user-agent,x-grpc-web,grpc-timeout
                        expose_headers: ${ENVOY_EXPOSE_HEADERS},grpc-status-details-bin,grpc-status,grpc-message
                        max_age: "1728000"
                http_filters:
                  - name: envoy.filters.http.grpc_web
                  - name: envoy.filters.http.cors
                  - name: envoy.filters.http.router
  clusters:
    - name: ${ENVOY_TARGET_NAME}
      type: logical_dns
      connect_timeout: 0.25s
      lb_policy: round_robin
      http2_protocol_options: {}
      load_assignment:
        cluster_name: ${ENVOY_TARGET_NAME}
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      { address: ${ENVOY_TARGET_ADDR}, port_value: ${ENVOY_TARGET_PORT} }

layered_runtime:
  layers:
    - name: static_layer_0
      static_layer:
        envoy:
          resource_limits:
            listener:
              listener_0:
                connection_limit: 10000
        overload:
          global_downstream_max_connections: 50000
